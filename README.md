# Clouvel

> **PRD 없으면 코딩 없다.**

바이브코딩 프로세스를 강제하는 MCP 서버.
문서 없이 코딩 시작? 차단됩니다.

---

## 설치

```bash
pip install clouvel
```

또는

```bash
uvx clouvel
```

---

## Claude Code 연동

```json
{
  "mcpServers": {
    "clouvel": {
      "command": "uvx",
      "args": ["clouvel"]
    }
  }
}
```

---

## 도구 목록 (26개)

### Core (4개)

#### `can_code`
코드 작성 전 반드시 호출. 문서 상태 확인 후 코딩 가능 여부 판단.

```
입력: { "path": "프로젝트/docs" }
출력:
  ✅ 코딩 가능 - 모든 필수 문서 존재
  ❌ 코딩 차단 - PRD.md 없음
```

**예시:**
```
> can_code({ "path": "./docs" })

❌ 코딩 차단
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✗ PRD.md - 없음
✗ Architecture.md - 없음
✓ API.md - 존재
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 먼저 docs/PRD.md를 작성하세요.
```

---

#### `scan_docs`
프로젝트 docs 폴더 스캔. 파일 목록 반환.

```
입력: { "path": "docs 폴더 경로" }
출력: 파일 목록 + 메타데이터
```

**예시:**
```
> scan_docs({ "path": "./docs" })

📁 docs/ 스캔 결과
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- PRD.md (2.3KB, 2026-01-15)
- API.md (1.1KB, 2026-01-14)
- PLAN.md (800B, 2026-01-15)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
총 3개 파일
```

---

#### `analyze_docs`
docs 폴더 분석. 필수 문서 체크 + 품질 점수.

```
입력: { "path": "docs 폴더 경로" }
출력: 분석 결과 + 권장사항
```

**예시:**
```
> analyze_docs({ "path": "./docs" })

📊 문서 분석 결과
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
필수 문서: 3/5 (60%)
✓ PRD.md
✓ API.md
✗ Architecture.md
✗ DB_Schema.md
✗ Verification.md
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 Architecture.md 작성을 권장합니다.
```

---

#### `init_docs`
docs 폴더 초기화 + 템플릿 생성.

```
입력: {
  "path": "프로젝트 루트 경로",
  "project_name": "프로젝트 이름"
}
출력: 생성된 파일 목록
```

**예시:**
```
> init_docs({ "path": ".", "project_name": "MyApp" })

✅ docs/ 초기화 완료
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
생성된 파일:
- docs/PRD.md (템플릿)
- docs/Architecture.md (템플릿)
- docs/API.md (템플릿)
- docs/PLAN.md (빈 파일)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 PRD.md부터 작성을 시작하세요.
```

---

### Docs (6개)

#### `get_prd_template`
PRD 템플릿 생성.

```
입력: {
  "project_name": "프로젝트 이름",
  "output_path": "출력 경로"
}
출력: PRD 템플릿 마크다운
```

**예시:**
```
> get_prd_template({ "project_name": "TaskManager", "output_path": "./docs/PRD.md" })

# TaskManager PRD

## 1. 개요
[프로젝트 한 줄 설명]

## 2. 고객 문제
[해결하려는 문제]

## 3. 핵심 기능
- [ ] 기능 1
- [ ] 기능 2

...
```

---

#### `write_prd_section`
PRD 섹션별 작성 가이드.

```
입력: {
  "section": "summary | principles | input_spec | output_spec | errors | state_machine | api_endpoints | db_schema",
  "content": "섹션 내용 (선택)"
}
출력: 해당 섹션 작성 가이드
```

**예시:**
```
> write_prd_section({ "section": "input_spec" })

📝 Input Spec 작성 가이드
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
필수 항목:
1. 입력 데이터 타입
2. 필수/선택 필드 구분
3. 유효성 검증 규칙
4. 예시 데이터

템플릿:
| 필드 | 타입 | 필수 | 설명 |
|------|------|------|------|
| name | string | ✓ | 사용자 이름 |
```

---

#### `get_prd_guide`
PRD 작성 전체 가이드.

```
입력: 없음
출력: PRD 작성 가이드 전문
```

---

#### `get_verify_checklist`
검증 체크리스트.

```
입력: 없음
출력: 검증 체크리스트
```

**예시:**
```
> get_verify_checklist()

✅ 검증 체크리스트
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
□ PRD 요구사항 모두 구현?
□ 엣지 케이스 처리?
□ 에러 핸들링 완료?
□ 테스트 작성?
□ 문서 업데이트?
```

---

#### `get_setup_guide`
설치/설정 가이드.

```
입력: { "platform": "desktop | code | vscode | cursor | all" }
출력: 플랫폼별 설정 가이드
```

---

#### `get_analytics`
도구 사용량 통계.

```
입력: {
  "path": "프로젝트 경로",
  "days": 30
}
출력: 사용량 통계
```

**예시:**
```
> get_analytics({ "path": ".", "days": 7 })

📊 Clouvel 사용 통계 (최근 7일)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
총 호출: 47회
가장 많이 사용: can_code (23회)
Gate 통과율: 85%

도구별:
- can_code: 23회
- verify: 12회
- gate: 8회
- handoff: 4회
```

---

### Setup (2개)

#### `init_clouvel`
Clouvel 온보딩. 플랫폼 선택 후 맞춤 설정.

```
입력: { "platform": "desktop | vscode | cli | ask" }
출력: 온보딩 결과
```

**예시:**
```
> init_clouvel({ "platform": "cli" })

🚀 Clouvel 온보딩 완료
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
플랫폼: CLI (Claude Code)
생성된 파일:
- .clouvel/config.json
- .clouvel/analytics.json
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 이제 can_code를 호출해보세요.
```

---

#### `setup_cli`
CLI 환경 설정. hooks, CLAUDE.md, pre-commit 생성.

```
입력: {
  "path": "프로젝트 루트 경로",
  "level": "remind | strict | full"
}
출력: 설정 결과
```

**예시:**
```
> setup_cli({ "path": ".", "level": "strict" })

⚙️ CLI 설정 완료 (strict 모드)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
생성:
- CLAUDE.md (규칙 파일)
- .git/hooks/pre-commit

strict 모드:
- PRD 없으면 커밋 차단
- can_code 실패 시 경고
```

---

### Rules - v0.5 (3개)

#### `init_rules`
규칙 모듈화 초기화.

```
입력: {
  "path": "프로젝트 루트 경로",
  "template": "web | api | fullstack | minimal"
}
출력: 규칙 파일 생성 결과
```

**예시:**
```
> init_rules({ "path": ".", "template": "web" })

📜 규칙 초기화 완료 (web)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
생성된 규칙:
- .clouvel/rules/frontend.md
- .clouvel/rules/api.md
- .clouvel/rules/security.md
```

---

#### `get_rule`
경로 기반 규칙 로딩.

```
입력: {
  "path": "파일 경로",
  "context": "coding | review | debug | test"
}
출력: 해당 파일/컨텍스트에 맞는 규칙
```

**예시:**
```
> get_rule({ "path": "src/api/auth.ts", "context": "coding" })

📜 적용 규칙 (api/auth)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
NEVER:
- 비밀번호 평문 저장
- JWT 시크릿 하드코딩

ALWAYS:
- bcrypt로 해싱
- 환경변수에서 시크릿 로드
```

---

#### `add_rule`
새 규칙 추가.

```
입력: {
  "path": "프로젝트 루트 경로",
  "rule_type": "never | always | prefer",
  "content": "규칙 내용",
  "category": "api | frontend | database | security | general"
}
출력: 추가 결과
```

**예시:**
```
> add_rule({
    "path": ".",
    "rule_type": "never",
    "content": "console.log를 프로덕션에 남기지 않는다",
    "category": "frontend"
  })

✅ 규칙 추가됨
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
타입: NEVER
카테고리: frontend
내용: console.log를 프로덕션에 남기지 않는다
```

---

### Verify - v0.5 (3개)

#### `verify`
Context Bias 제거 검증.

```
입력: {
  "path": "검증 대상 경로",
  "scope": "file | feature | full",
  "checklist": ["항목1", "항목2"]
}
출력: 검증 결과
```

**예시:**
```
> verify({ "path": "src/auth", "scope": "feature" })

🔍 검증 결과 (auth 기능)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✓ PRD 요구사항 충족
✓ 에러 핸들링 존재
✗ 테스트 커버리지 부족 (60%)
✗ 문서 미업데이트
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
결과: 2/4 통과
💡 테스트 추가 후 재검증하세요.
```

---

#### `gate`
lint → test → build 자동화.

```
입력: {
  "path": "프로젝트 루트 경로",
  "steps": ["lint", "test", "build"],
  "fix": true/false
}
출력: Gate 결과
```

**예시:**
```
> gate({ "path": ".", "steps": ["lint", "test", "build"], "fix": true })

🚦 Gate 실행
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Lint... ✅ PASS (2 auto-fixed)
2. Test... ✅ PASS (12/12)
3. Build.. ✅ PASS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎉 Gate PASS - 커밋 가능
```

---

#### `handoff`
의도 기록 (다음 세션을 위한 컨텍스트 저장).

```
입력: {
  "path": "프로젝트 루트 경로",
  "feature": "완료한 기능",
  "decisions": "결정 사항",
  "warnings": "주의사항",
  "next_steps": "다음 할 일"
}
출력: 핸드오프 문서
```

**예시:**
```
> handoff({
    "path": ".",
    "feature": "로그인 기능",
    "decisions": "JWT 선택 (세션 대신)",
    "warnings": "refresh token 아직 미구현",
    "next_steps": "회원가입 기능"
  })

📋 핸드오프 저장됨
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
기능: 로그인 기능
결정: JWT 선택 (세션 대신)
⚠️ 주의: refresh token 아직 미구현
다음: 회원가입 기능
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 /clear 후 이 내용을 참고하세요.
```

---

### Planning - v0.6 (4개)

#### `init_planning`
영속적 컨텍스트 초기화.

```
입력: {
  "path": "프로젝트 루트 경로",
  "task": "현재 작업",
  "goals": ["목표1", "목표2"]
}
출력: 초기화 결과
```

**예시:**
```
> init_planning({
    "path": ".",
    "task": "결제 시스템 구현",
    "goals": ["Stripe 연동", "웹훅 처리", "영수증 발송"]
  })

🎯 Planning 초기화
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
작업: 결제 시스템 구현
목표:
  1. Stripe 연동
  2. 웹훅 처리
  3. 영수증 발송
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 refresh_goals로 언제든 리마인드
```

---

#### `save_finding`
조사 결과 저장.

```
입력: {
  "path": "프로젝트 루트 경로",
  "topic": "주제",
  "question": "질문",
  "findings": "발견한 내용",
  "source": "출처",
  "conclusion": "결론"
}
출력: 저장 결과
```

**예시:**
```
> save_finding({
    "path": ".",
    "topic": "Stripe 웹훅",
    "findings": "signature 검증 필수, 재시도 로직 있음",
    "source": "Stripe 공식 문서",
    "conclusion": "stripe.webhooks.constructEvent 사용"
  })

💾 Finding 저장됨
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
주제: Stripe 웹훅
결론: stripe.webhooks.constructEvent 사용
파일: .clouvel/findings/stripe-webhook.md
```

---

#### `refresh_goals`
목표 리마인드.

```
입력: { "path": "프로젝트 루트 경로" }
출력: 현재 목표 + 진행 상황
```

**예시:**
```
> refresh_goals({ "path": "." })

🎯 현재 목표 리마인드
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
작업: 결제 시스템 구현

목표:
  ✅ Stripe 연동
  🔄 웹훅 처리 (진행 중)
  ⬚ 영수증 발송

진행률: 33%
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 웹훅 처리에 집중하세요.
```

---

#### `update_progress`
진행 상황 업데이트.

```
입력: {
  "path": "프로젝트 루트 경로",
  "completed": ["완료 항목"],
  "in_progress": "진행 중",
  "blockers": ["막힌 부분"],
  "next": "다음 할 일"
}
출력: 업데이트 결과
```

**예시:**
```
> update_progress({
    "path": ".",
    "completed": ["Stripe 연동", "웹훅 처리"],
    "in_progress": "영수증 발송",
    "blockers": [],
    "next": "테스트 작성"
  })

📊 진행 상황 업데이트
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
완료: Stripe 연동, 웹훅 처리
진행 중: 영수증 발송
다음: 테스트 작성
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
진행률: 66% → 계속 진행!
```

---

### Agents - v0.7 (2개)

#### `spawn_explore`
탐색 전문 에이전트. 코드베이스 탐색.

```
입력: {
  "path": "프로젝트 루트 경로",
  "query": "탐색 질문",
  "scope": "file | folder | project | deep",
  "save_findings": true/false
}
출력: 탐색 결과
```

**예시:**
```
> spawn_explore({
    "path": ".",
    "query": "인증 로직이 어디에 있나?",
    "scope": "project",
    "save_findings": true
  })

🔍 Explore 에이전트 결과
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
질문: 인증 로직이 어디에 있나?

발견:
- src/lib/auth.ts (메인 인증 로직)
- src/middleware/auth.ts (미들웨어)
- src/api/auth/route.ts (API 엔드포인트)

관련 파일: 3개
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💾 findings에 저장됨
```

---

#### `spawn_librarian`
라이브러리언 에이전트. 외부 문서/API 조사.

```
입력: {
  "path": "프로젝트 루트 경로",
  "topic": "조사 주제",
  "type": "library | api | migration | best_practice",
  "depth": "quick | standard | thorough"
}
출력: 조사 결과
```

**예시:**
```
> spawn_librarian({
    "path": ".",
    "topic": "Next.js 15 App Router",
    "type": "migration",
    "depth": "standard"
  })

📚 Librarian 에이전트 결과
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
주제: Next.js 15 App Router

핵심 변경점:
- async/await 서버 컴포넌트 기본
- 새 캐싱 전략
- Turbopack 안정화

마이그레이션 가이드:
1. next.config.js 업데이트
2. getServerSideProps → 서버 컴포넌트
3. _app.tsx → layout.tsx

출처: Next.js 공식 문서
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💾 findings에 저장됨
```

---

### Hooks - v0.8 (2개)

#### `hook_design`
설계 훅 생성. 코딩 전 자동 체크.

```
입력: {
  "path": "프로젝트 루트 경로",
  "trigger": "pre_code | pre_feature | pre_refactor | pre_api",
  "checks": ["체크 항목"],
  "block_on_fail": true/false
}
출력: 훅 생성 결과
```

**예시:**
```
> hook_design({
    "path": ".",
    "trigger": "pre_api",
    "checks": ["PRD에 엔드포인트 정의됨?", "인증 방식 결정됨?"],
    "block_on_fail": true
  })

🪝 설계 훅 생성됨
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
트리거: pre_api
체크:
  □ PRD에 엔드포인트 정의됨?
  □ 인증 방식 결정됨?
실패 시: 차단
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 API 작성 전 자동으로 체크됩니다.
```

---

#### `hook_verify`
검증 훅 생성. 코딩 후 자동 검증.

```
입력: {
  "path": "프로젝트 루트 경로",
  "trigger": "post_code | post_feature | pre_commit | pre_push",
  "steps": ["lint", "test", "build"],
  "parallel": true/false,
  "continue_on_error": true/false
}
출력: 훅 생성 결과
```

**예시:**
```
> hook_verify({
    "path": ".",
    "trigger": "pre_commit",
    "steps": ["lint", "test"],
    "parallel": true,
    "continue_on_error": false
  })

🪝 검증 훅 생성됨
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
트리거: pre_commit
단계: lint, test (병렬 실행)
실패 시: 커밋 차단
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 커밋 전 자동으로 검증됩니다.
```

---

## Pro 버전

더 강력한 기능이 필요하다면 **Clouvel Pro**를 확인하세요.

- **Shovel 워크플로우** 자동 설치
- **Gate 시스템** (lint → test → build)
- **검증 프로토콜** (Boris 방식)
- **에러 학습 시스템**
- **Context 관리 도구**

**[Clouvel Pro 보러가기](https://whitening-sinabro.github.io/clouvel/)**

---

## 링크

- [GitHub](https://github.com/Whitening-Sinabro/clouvel)
- [Landing Page](https://whitening-sinabro.github.io/clouvel/)
- [Issues](https://github.com/Whitening-Sinabro/clouvel/issues)

---

## 라이선스

MIT
